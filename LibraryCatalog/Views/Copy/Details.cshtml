@{
    Layout = "_Layout";
}

<div class="row">
    <div class="col-sm-12">
        <h3>@Model.Name:</h3>
        <hr /><br />
        <h4>@Model.Name</h4>
        <h5><strong>Books</strong></h5>
        @foreach(var book in Model.GetBooks())
        {
            <p><a href="~/book/@book.Id">@book.Name</a></p>
        }
        <a href="/view/copies/@Model.Id/update" class="btn btn-primary">Edit</a>
        <form method="post" class="inline-form-button" action="/view/copies/@Model.Id/delete"><button type="submit" class="btn btn-danger">Delete</button></form>
        <hr />
    </div>
</div>
