@{
    Layout = "_Layout";
}

<div class="row">
    <div class="col-sm-12">
        <h3>@Model.Title:</h3>
        <hr /><br />
        <h5><strong>Authors:</strong></h5>
        @foreach(var author in Model.GetAuthors())
        {
            <p><a href="~/view/authors/@author.Id">@author.Name</a></p>
        }
        <a href="/view/book/@Model.Id/update" class="btn btn-primary">Edit</a>
        <form method="post" class="inline-form-button" action="/view/book/@Model.Id/delete"><button type="submit" class="btn btn-danger">Delete</button></form>
        <hr />
    </div>
</div>
